{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for controls and overlay windows",
  "windows": ["controls", "overlay"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default",
    "dialog:default",
    "autostart:default",
    "opener:default"
  ]
}
